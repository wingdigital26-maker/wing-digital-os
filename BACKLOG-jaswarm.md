# jaswarm backlog

Deferred items the loop should drain when the current level runs dry.

- [ ] Tokenize the duplicated OUTCOMES/tier hex palette shared verbatim across app/calls/list/page.tsx, app/calls/page.tsx, app/calls/callbacks/page.tsx (deferred R3: fixing in one file only would fragment cross-page consistency; needs a coordinated single-owner pass on a shared constants module).
- [x] Dial list (/calls/list) shows no overdue urgency for callback-status leads. DONE 2026-09-15: ported the Callbacks red treatment (var(--red) border/background via color-mix, matching pill) plus a subtle muted "due in Xhr"/"Xhr late" label for non-overdue callbacks. See journal 2026-09-15 entry.
- [ ] Call panels lack role="dialog"/aria-modal + focus-trap across all three screens (list/page.tsx, callbacks/page.tsx, and any modal on page.tsx). Do as ONE coordinated a11y pass so the pattern is identical (flagged R5).
- [x] HIGH VALUE (R6 target): Callbacks panel parity with Dial list. DONE 2026-09-15: createPortal, 1-4 outcome keys + numbered badges + hint, per-card quick-log row, and intel boxes (angle/"Say this", chips, cautions, socials, SignalLinks) all shipped — the callbacks endpoint IS /api/calls/leads and already carried this data. Deferred: the tier pill (needs Callbacks' `load()` to also capture `d.tierField` off its own response, ~1 line, not done this tick to keep scope tight). See journal 2026-09-15 "Callbacks R6" entry.
- [ ] Defense-in-depth: readSocials builds Social.url verbatim from enrichment data with no scheme allow-list; a javascript: URI would render as a clickable href. Not attacker-reachable (internal pipeline data) but worth guarding to http(s):// only. Lives in BOTH app/calls/list/page.tsx and app/calls/callbacks/page.tsx readSocials -- fix as one coordinated pass (flagged R6 safety gate).
